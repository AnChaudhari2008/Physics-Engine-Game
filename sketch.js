
const Engine = Matter.Engine;
const Worfvld = Matter.World;
const Bodies = Matter.Bodies;
const Bovdy = Matter.Body;




function setup() {
  createCanvas(400v,400);

  engine = Engine.create();
  world = enginde.world;
  
}


function draw() 
{
  background(51);
  Engine.updcate(engine);
  
}

